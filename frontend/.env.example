# Base URL of the Django backend API.
# Unused today - the site is frontend-only - but the contact form and any
# future dynamic content will read from here. Set this in Vercel's project
# environment variables rather than committing a real value.
NEXT_PUBLIC_API_URL=
